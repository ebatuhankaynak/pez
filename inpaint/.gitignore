# heavy intermediates + media
.work_*/
__pycache__/
*.mp4
!input/*.mp4

# downloaded model weights (big-lama etc. — regenerable)
.cache/

# eval run outputs + scratch montages (regenerable via batch_eval.py)
eval/out/
eval/manifest.json
eval/_*
