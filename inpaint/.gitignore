# heavy intermediates + media
.work_*/
__pycache__/
*.mp4
!input/*.mp4

# downloaded model weights (big-lama etc. — regenerable)
.cache/

# eval run outputs + scratch montages (all regenerable via batch_eval.py)
eval/

# vendored MiniMax-Remover (own git repo + ~2.6G HF weights) — provisioned
# separately, see README "MiniMax engine setup". Never committed here.
_minimax/

# ad-hoc diagnostics (gate/flicker probes) — scratch, regenerable
_diag/
