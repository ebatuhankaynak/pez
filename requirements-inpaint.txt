# Track 2 (caption inpainting) — LaMa engine deps. These are the *pure-wheel* transitive
# deps of rapidocr-onnxruntime that the base image lacks. onnxruntime-gpu, opencv, pillow
# and numpy are already installed by the layers above and are deliberately NOT repeated
# here: a second `onnxruntime`/`opencv-python` would collide with the baked builds, and an
# older Pillow pin would force a source build (this image has no zlib headers). For the
# same reason rapidocr-onnxruntime + simple-lama-inpainting themselves are installed
# --no-deps in the Dockerfile, right after this file.
pyclipper==1.4.0
shapely==2.1.2
PyYAML==6.0.3
six==1.17.0
